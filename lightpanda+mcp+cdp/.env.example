# Copy this file to .env and adjust values if needed.

LIGHTPANDA_MCP_PORT=8931

# Bearer token for the Lightpanda MCP endpoint.
# Required by the nginx container. Leave empty to disable token checks (not recommended).
MCP_AUTH_TOKEN=

# Disable Lightpanda telemetry.
LIGHTPANDA_DISABLE_TELEMETRY=true
